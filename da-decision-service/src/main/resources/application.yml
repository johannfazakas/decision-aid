server:
  port: 7032
spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      platform: postgres
  datasource:
    url: jdbc:postgresql://da-postgres:5432/da_decision
    username: postgres
    password: admin
